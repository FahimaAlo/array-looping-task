const hobby = 'I am a hard working person';

// Split the sentence into words, reverse the order, and join them back into a string
const reversedWords = hobby.split(' ').reverse().join(' ');

console.log(reversedWords); // Output: 'person working hard a am I'
